import { CourseSectionWidget } from '@/widgets/course-section/ui/CourseSectionWidget/CourseSectionWidget.js';

export function CoursesPage() {
  return (
    <section>
      <CourseSectionWidget showTitle={false} />
    </section>
  );
}
